spring:
  application:
    name: phase2-activemq-demo
  profiles:
    # 默认本地开发；生产请用 --spring.profiles.active=prod 或环境变量 SPRING_PROFILES_ACTIVE
    active: dev

# 各环境共用的业务默认（无需随 Broker/库变化而改的参数）
demo:
  idempotency:
    redis-key-prefix: "idem:"
    ttl-seconds: 86400
